# 更新日志

本项目版本号与皮肤系统内的 `SKIN_VERSION` 一致，客户端「检查更新」比对的就是本仓库 Releases 里的最新 tag。

## v1.0.0 — 2026-08-14

首个公开版本。

**皮肤引擎**

- 数据源驱动：一套皮肤等于一份 `themes/<id>.json`，四个色种（accent / secondary / surface / text）加外观和边框风格，构建期生成 CSS，运行期只切一个 `data-dsh-skin` 属性。
- 保对比度推导：从色种推出完整色阶时保持与原生色板相同的对比度关系，不是简单地把颜色换掉。OKLab 色彩空间计算，色域外的颜色用色度二分法拉回 sRGB。
- 三种边框风格：`flat`（实色描边）、`glass`（毛玻璃）、`neon`（发光描边），随皮肤定义走。
- 可读性契约：21 套皮肤 × 8 项对比度检查在构建期确定性校验，跑 `pnpm --filter @deepseek-ai/dsh-client-ui-theme run check:skins` 复现。
- 作用域收口：全部规则挂在 `body[data-dsh-skin]` 下，不改全局变量，取消皮肤即刻回到原生外观。

**内置 21 套皮肤**

QQ 2007、QQ 2008·粉、初音未来、原神·蒙德、原神·璃月夜、恋与深空·星海、恋与深空·星际、火影·鸣人、火影·佐助、鸣潮·黑青、鸣潮·深紫、龙珠·筋斗云、龙珠·超赛、大佬·烟灰、DeepSeek 娘·深海、DeepSeek 娘·Q 版、DeepSeek 青春版、内测大佬、别影响 AGI、峰哥骑鲸、梁圣·静音。

**自定义皮肤**

- 选择器第一项就是「自定义（选图）」，任意 PNG / JPG / WebP 拖进来即可。
- 浏览器端解码一次，96px 采样取色，1920px 长边重编码成 WebP 再上传，原图不出本机。
- 从图里提主色、副色、底色、文字色，自动判深浅外观，自动按画面明暗调蒙版浓度，整套配色跟着图走。
- 图片存 `~/.dsh/skins/`，按内容哈希命名，Host 加了只读静态路由，同一张图重复上传不会存两份。

**版本与更新检查**

- 设置面板底部显示「皮肤系统 vX.Y.Z · DSH <宿主版本>」。
- 「检查更新」走 Host 侧代理请求 GitHub Releases，3 秒超时、64KB 上限、60 秒缓存，浏览器不直连第三方。

**修复**

- 背景图在开启对话时被强制放大：背景层从会随内容缩放的容器里挪出来，固定在视口上，滚动和开合会话都不再位移。
